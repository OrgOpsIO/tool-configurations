# Domain-Konfiguration für Open WebUI
DOMAIN_NAME=orgops.io
SUBDOMAIN=chat

# Open WebUI Konfiguration
# Secret Key (mindestens 12 Zeichen empfohlen)
# Generiere mit: openssl rand -hex 32
WEBUI_SECRET_KEY=your-secret-key-here-generate-with-openssl

# Name der Open WebUI Instanz
WEBUI_NAME=Open WebUI

# Optional: Ollama API Endpoint
# Falls Ollama auf dem Host läuft: http://host.docker.internal:11434
# Falls Ollama in Docker läuft: http://ollama:11434
# Falls Ollama auf einem anderen Server läuft: http://SERVER_IP:11434
OLLAMA_BASE_URL=
